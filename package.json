{
  "name": "linux-activity-tracker",
  "version": "1.0.50",
  "module": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --outdir ./dist --target bun",
    "check": "tsc --noEmit",
    "test:db": "bun run test-db.ts",
    "test:ram": "bun run test-ram.ts"
  },
  "dependencies": {
    "elysia": "latest",
    "@elysiajs/graphql-yoga": "^1.1.2",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
